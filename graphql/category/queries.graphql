extend type Query {
    category(
        id: ID! @globalId(decode: ID) @eq
    ): Category @find

    categories(
        productsId: ID @globalId(decode: ID) @eq(key: "product_id")
    ): [Category!]! @paginate(type: CONNECTION)
}
